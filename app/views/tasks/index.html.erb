<h1>All Tasks</h1>
<ul>
<% @tasks.each do |task| %>
 <li><%= task.completed ? "✅" : "❌" %><%=link_to task.title, task_path(task)%> <%= link_to "delete", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %></li>
 <% end %>
</ul>

<p><%= link_to "Add a new Task", new_task_path %></p>
